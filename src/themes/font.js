const fontSizes = {
  xs: 12,
  sm: 14,
  md: 16,
  lg: 24,
  xl: 32,
  '2xl': 60
};

const fontWeights = {
  regular: 400,
  medium: 500,
  semiBold: 600,
};

// CommonJS Module Export for Tailwindcss
module.exports.fontSizes = fontSizes;
module.exports.fontWeights = fontWeights;
