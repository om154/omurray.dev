# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on push to master
'on': push
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    env:
      REACT_APP_DATE_OF_BIRTH: ${{ secrets.REACT_APP_DATE_OF_BIRTH }}
      REACT_APP_USER_EMAIL_ADDRESS: ${{ secrets.REACT_APP_USER_EMAIL_ADDRESS }}
      REACT_APP_GITHUB_URL: ${{ secrets.REACT_APP_GITHUB_URL }}
      REACT_APP_LINKEDIN_URL: ${{ secrets.REACT_APP_LINKEDIN_URL }}
      REACT_APP_TWITTER_URL: ${{ secrets.REACT_APP_TWITTER_URL }}
    steps:
      - uses: actions/checkout@v2
      - run: |
          yarn
          yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PERSONAL_WEBSITE_A0D1B }}'
          projectId: personal-website-a0d1b
          channelId: live
